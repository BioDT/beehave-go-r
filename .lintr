linters: linters_with_defaults(
    line_length_linter(length = 120L),
    commented_code_linter = NULL,
    indentation_linter = NULL) # see vignette("lintr")
exclusions:
    list(
        "dev",
        ".Rprofile"
    )
encoding: "UTF-8"
