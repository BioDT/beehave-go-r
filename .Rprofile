source("renv/activate.R")

options(
    # activate RStudio Addins on command pallet
    vsc.rstudioapi = TRUE,
    # radian highlight scheme (choose what suits you)
    radian.color_scheme = "native",
    # code completion triggers
    vsc.code_completion_triggers = list(
        list(
            trigger = ".",
            kind = "keyword"
        )
    )
)
